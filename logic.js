const typed = new Typed('.multiple-text', {
    strings: ['About', 'Me!:'],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});

const typed1 = new Typed('.multiple-text1', {
    strings: ['Internships:', 'and', 'Training:'],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});
